# Copy to `.env` (gitignored) and fill in.
# Supabase dashboard -> Project Settings -> Database -> Connection string.
# Use the direct connection or session pooler string (includes the DB password).
SUPABASE_DB_URL=
